# Catalog — Templates verticaux Scalario

Placeholder (STORY-013). Le template `retail_fresh_produce.json` (mémoire projet) sera ajouté dans `domains/retail/fusions/` (story dédiée EPIC-004 / EPIC-005).

```
catalog/
├── domains/    # Verticaux métier (retail, fresh-produce, services, etc.)
├── modules/    # Modules réutilisables (catalogue, inventory, sales, etc.)
├── fusions/    # Compositions de modules (templates clients)
└── schemas/    # JSON Schemas validation par type
```
